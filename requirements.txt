# Runtime + dev dependencies. Mirrors pyproject.toml — `pip install -e .[dev]` is
# equivalent. Kept for `pip install -r requirements.txt` on a fresh machine.
beautifulsoup4>=4.12.0
requests>=2.31.0
spacy>=3.7.0
scikit-learn>=1.4.0
numpy>=1.26.0
streamlit>=1.32.0
pytest>=8.0
ruff>=0.3.0

# Optional: embedding-similarity teammates (ranked last, behind the synergy rules).
# Pulls in torch (~2GB). Without it the pipeline runs rule-only — see --no-embeddings.
# sentence-transformers>=2.6.0
